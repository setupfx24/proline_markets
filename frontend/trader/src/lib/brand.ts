/** Dashboard / in-app product name (landing & marketing stay ProlineMarkets). */
export const BRAND_NAME = 'ProlineMarkets';

/** Zustand persist key for UI preferences (theme, terminal layout). */
export const STORAGE_KEY_UI = 'ProlineMarkets-ui';

/** Legacy key â€” migrated once on load; logout clears legacy auth key. */
export const STORAGE_KEY_UI_LEGACY = 'ProlineMarkets-ui';
