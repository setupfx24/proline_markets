# Dependencies
node_modules/
**/node_modules/

# Next.js
.next/
out/
**/.next/

# Python
venv/
.venv/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
**/venv/

# Env & secrets (never commit)
.env
.env.*
!.env.example
*.pem

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.cursor/
.claude/
.superdesign/

# Vite dep cache (auto-regenerated)
.vite/
**/.vite/

# Logs & debug
*.log
*.txt.log
admin_logs.txt
npm-debug.log*
yarn-debug.log*

# Upload storage (use object storage in production)
# CRITICAL: contains user PII (KYC passport scans, deposit proofs) — never commit
/uploads/
backend/uploads/**
!backend/uploads/**/.gitkeep
!backend/uploads/.gitkeep

# Database backups
/backups/

# Test / coverage
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Build
dist/
build/
*.egg-info/

# Qt desktop terminal — track src/ + web app code + build scripts only.
# build/ and dist/ are already covered above; these are the heavy leftovers
# (build-msvc ~335 MB of MSVC objects + Qt DLLs, vendor ~26 MB charting lib).
client_terminal/build-msvc/
client_terminal/web/vendor/

# Misc
*.local
*.tsbuildinfo
